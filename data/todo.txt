---TODO---




---OBJECTS---
- need to store objects in dictionary in the same was buildings work to block invalid placements





---BUILDINGS---
- refactor buildings to use components 
- ghost buildings show producing resources, need to disable if not placed
- need to populate occupied cells if placing buildings pre running
		The fix is in the building's _ready() — when a building is added to the scene, it should register itself in BuildManager.occupied_cells automatically. That way it doesn't matter whether it was placed at runtime or pre-populated in the editor.
		Think about what the building needs to do in _ready() to register itself:

		It knows its own grid_size
		It knows its own global_position
		It can convert that position to a grid cell via GridHelper
		It can call BuildManager.mark_cell_occupied()
- rework mesh system as removing navigation
- stop ghost buildings shaking and placement sound 
